```csharp title:Algoritmo_Greedy
	#Inizializzazione variabili decisionali
	float[] X = vector(float, R);
	int[] N = vector(int, R);
	list<node>(V)[] P = vector(list<node>(V), R) 
	
	#Valutazione di X

	for (r=0;r<R;r++)
	{
		X(r) = rho(r);
	}
	
	# Valutazione del Routing
	while(num_righe(N) < length(V_rifiuto))
	{
	
	}
	
```


